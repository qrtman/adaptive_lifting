import type { LocalAthlete } from '../../types';
import { ZAHAR_ATHLETE } from './zaharBlock31';

/** Roster identities the app ships with so a coach has something to open offline. */
export const SEEDED_ATHLETES: LocalAthlete[] = [ZAHAR_ATHLETE];
