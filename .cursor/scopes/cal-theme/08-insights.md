# Scope 08 — Insights (Phase B)

Load this for Insights cards. Depends on 01, 02, 10.

## Task

Insight cards on Cal `feature-card` / `surface-card`. Nested config inside a card is allowed (level 2). Empty/loading/error required.

## Locked

- Presets stay data, not new chart libraries for theme.
- Offline flush `mutation_type: insight_card` unchanged.
- No marketing testimonial layout.

## Files

- `src/components/InsightsView.tsx`
- `src/insights/*`

## Out of scope

New analytics endpoints.
